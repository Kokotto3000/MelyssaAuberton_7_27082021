import EventsManager from "./model/EventsManager.js";

//lancer l'affichage de toutes les recettes par défaut ? certaines en exemple (coco)... que l'affichage de la section ne soit pas vide...
const events= new EventsManager();
events.init();